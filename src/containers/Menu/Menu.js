import React from 'react';

import './sass/Menu.sass';
import Header from '../../components/Menu/Header';

const Menu = () => {
    return (
        <div className="menu">
            <Header/>
        </div>
    );
};

export default Menu;
